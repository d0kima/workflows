# Created by Janin Michel-Mathias on 2026-04-09

on:
    workflow_call:
        inputs:
            version:
                required: true
                type: string
            is_release:
                required: false
                type: boolean
                default: true

jobs:
    publish-to-github-packages:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout repository
              uses: actions/checkout@v3

            - name: Set up Node.js
              uses: actions/setup-node@v3
              with:
                  node-version: '24'

            - name: Set up .npmrc for Nexus registry
              run: |
                  echo "@d0kima:registry=https://nexus.d0kima.com/repository/npm-registry/" > .npmrc
                  echo "//nexus.d0kima.com/repository/npm-registry/:_auth=$(echo -n ${{ secrets.NEXUS_USERNAME }}:${{ secrets.NEXUS_PASSWORD }} | base64)" >> .npmrc
                  echo "//nexus.d0kima.com/repository/npm-registry/:always-auth=true" >> .npmrc

            - name: Publish to Nexus Registry
              run: npm publish --registry https://nexus.d0kima.com/repository/npm-registry/
              env:
                  NODE_AUTH_TOKEN: ${{ secrets.PACKAGES_TOKEN }}
    publish-release:
        if: ${{ inputs.is_release }}
        needs: publish-to-github-packages
        runs-on: ubuntu-latest
        permissions:
            contents: write

        steps:
            - name: Checkout repository
              uses: actions/checkout@v3

            - name: Mark existing release as stable
              uses: actions/github-script@v7
              with:
                  script: |
                      const tag = `${{ inputs.version }}`;
                      const { owner, repo } = context.repo;

                      const { data: release } = await github.rest.repos.getReleaseByTag({
                          owner,
                          repo,
                          tag,
                      });

                      await github.rest.repos.updateRelease({
                          owner,
                          repo,
                          release_id: release.id,
                          prerelease: false,
                      });